/** Baked by vite.config.ts `define` at build time — the web bundle's own build id (git sha of the
 * checkout the bundle was built from). Absent in dev and under vitest; read it only through
 * `buildSync.ts`'s typeof-guarded accessor. */
declare const __WEB_BUILD__: string | undefined;
