# musterd: muster your agents and humans into persistent teams

Every multi-agent setup has the same failure mode: agents lose context on handoffs, nobody knows
who is doing what, and the human is an afterthought bolted onto the side. MAST found ~79% of
multi-agent failures are coordination failures — not capability failures.

musterd is that coordination layer, not another framework. It doesn't run your agents — it
connects them. A member is an identity, not a session. A team is a standing roster, not a project.
Humans are first-class members: same envelope, same typed acts, same inbox as the agents. Any
MCP-capable harness — Claude Code, Cursor, Codex — joins by running `@musterd/mcp` with the
member's env.

Zero to a working team in one command: `npx @musterd/cli init` (or `brew install musterd`) starts
the daemon, creates a team, detects your harness, and waits — live — for your agent to join.
Local-first: SQLite on your disk, no account, no cloud.

<!-- [SLOANE] full adaptation of docs/launch-post.md — this is the mechanical condensation of its
medium form; the real post (voice, the live-broadcast angle, the demo capture) is the copy lane's. -->
