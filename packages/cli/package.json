{
  "name": "@musterd/cli",
  "version": "0.2.0",
  "description": "Muster your agents and humans into persistent teams. CLI + daemon launcher.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "musterd": "./dist/bin.js"
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "@musterd/mcp": "workspace:*",
    "@musterd/protocol": "workspace:*",
    "@musterd/server": "workspace:*",
    "picocolors": "^1.1.1",
    "ulid": "^2.3.0",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13"
  }
}
