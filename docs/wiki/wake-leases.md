# Wake leases

A wake lease is discharged by the seat REPORTING the wake — not by answering it — so `lease_expired` means the wake never landed, never "nobody answered".

## The trap (2026-08-12; falsify: read the report path in packages/server/src/store/residency.ts)

Three cases want opposite responses: `expired` ⇒ the seat is unreachable, escalate immediately; `reported`-but-unanswered ⇒ they are on it, HOLD (any hold window must outlive `WAKE_LEASE_TTL_MS` = 120s); `decline` ⇒ "not me" is information, escalate immediately. Anything keying escalation on `lease_expired` alone escalates on the wrong signal and stays silent on the right one (recorded in ADR 254 Consequences for increment 2).

## The ledger prices only reported wakes (2026-08-08/12, ADR 252/#747; falsify: count wake_cost rows vs wake_leased)

`residency.wake_cost` is written only on the wake-report route, so a wake that spawns a session and dies `lease_expired` is paid and invisible — measured 35 cost rows against 150 `wake_leased`. Any dollar figure off this ledger is biased low in exactly the failure mode being priced; #747 counts these as `unpriced_sessions` OUTSIDE the totals, documented as a floor. Timing-window correlation is invalid on a flapping seat (ADR 241 removed exactly that inference). A handoff discharged by doing the work is recognized by `laneHandoffDischarged` in delivery.ts (#745 — ADR 090's two older clauses are reply-shaped and matched 0 of 8 real discharges); the exhaustion cap keys per act, and one handoff can be two envelopes, so 6 wakes is inside policy.

## Why the wakes above were unpriced — it was not the failure path (2026-08-14, ADR 269/#841; falsify: `grep "report failed" ~/.musterd/host.log`)

The section above is right that the ledger under-prices, and ~~its stated cause — no cost source exists on the failure path (2026-08-12)~~ CORRECTED 2026-08-14 by ADR 269: the cost source existed and worked. The daemon was **refusing the reports that carried it**. `transcript_age_ms` is `Date.now() - fs.stat().mtimeMs`, and `mtimeMs` is fractional on APFS (`1786744301066.2197`), so the value is a float against a `z.number().int()` wire schema. Zod rejects the whole object, so one unusable digit of sub-millisecond precision took down the lease settlement AND the `cost_usd` beside it: **48 rejected reports, $22.54 of harness-attested spend discarded**, each lease then expiring as `wake_failed {session_captured: true}` — the ledger calling a wake free that had just printed `cost=$1.3093` to stdout.

Two second-order effects worth knowing:

- **Unsettled acts get re-leased.** One act was leased 12 times; leases-per-act went 2.2 → 4.6 (08-04 → 08-13). Any lease-rate reading over that window — including the 0.23/h → 1.10/h rise the ADR 260 Eval attributed to ADR 253 routing — is contaminated by this defect in BOTH arms.
- ~~**A rejected report leaves no ledger trace at all.** The daemon bounced 48 reports and audited none of them, so the gap presented as "the host never answered". Only `tail ~/.musterd/host.log` shows otherwise. Still true as of 2026-08-14 (2026-08-14)~~ FIXED 2026-08-14 by ADR 273: a refused wake report now appends `residency.wake_report_rejected` carrying `{lease_id?, fields: [{path, code, expected?, received?}]}` — field paths and type names, never values — and `musterd report` prints a `refused` warning line. Falsify: `sqlite3 ~/.musterd/musterd.db "select count(*) from audit where action='residency.wake_report_rejected'"` after posting a wake report with `transcript_age_ms: -1`. Two gaps remain by design: malformed JSON and auth failures are refused before the schema and are still unaudited. See [instrument silence](instrument-silence.md).

The general trap: **do not put `.int()` on a number a filesystem handed you.** `fs.stat` returns IEEE doubles; `size` is integral but `mtimeMs`/`atimeMs`/`ctimeMs` are not. Round at the boundary (ADR 269's `HostMeasuredCount`), and keep strict integers for values a *caller chose* — `hourly_cap: 2.5` is a mistake worth refusing.

## The third cause — the backend never produced a completion, and the floor cannot see it (2026-09-02, lane 01M1G310Y7; falsify: `grep -c 'wake cost recorded for gptbot' ~/.musterd/host.log` against `grep -c 'portable delivery for gptbot'`)

The two sections above are about cost that was **produced and lost** — a lease that expired before the report (ADR 252), a report the daemon refused (ADR 269). This one is about cost that was **never produced**, and it has a per-harness shape, which is worse than an even undercount: it under-reports exactly where the pathology is.

Measured on the live host log, 2026-09-02:

| seat   | harness     | spawns | woke | `wake cost recorded` rows |
| ------ | ----------- | ------ | ---- | ------------------------- |
| dolly  | claude-code | 1      | 49   | 34                        |
| ryder  | claude-code | 40     | 52   | 22                        |
| miley  | claude-code | 4      | 25   | 18                        |
| izzo   | claude-code | 0      | 25   | 16                        |
| gptbot | codex       | 130    | 34   | **0**                     |

Two defects, one root. `codex.ts` and `opencode.ts` typed `settled: Promise<undefined>` — the run's completion promise resolved with nothing, on every run, so the loop's supplementary report (`loop.ts`, "wake cost recorded") had nothing to post and no codex or opencode wake has ever reached `residency.wake_cost`. And `claudeCode.ts` returned `undefined` from `settled` whenever stdout held no parseable JSON summary: **55 of 171** claude-code settles — 26 watchdog kills (`exit=143`), 18 `exit=error`, 7 `exit=0` with no summary, 4 killed resumes. The watchdog kill is the most expensive shape a wake can take (a live agent for the whole `timeout_ms`) and it priced at nothing. Both gated the record on the *child's self-report* when the *host* measures wall clock itself — the principle `native.ts` already states ("measured by the harness rather than self-reported by a child") and the other three backends had not applied.

**ADR 252's floor does not see these.** `unpriced_sessions` (`insights.ts`, ADR 252) counts rows with `session_captured: true` and no cost — a lease that *expired* after a session attested it. A wake that was reported `occupied` on its primary report and then never got a supplement is a `residency.woke` row with no cost and no `session_captured`: it is in `wake_count`, not in `cost_reported`, not in `unpriced_sessions`. gptbot's 34 woke leases all have this shape. So the honest reading of the ledger was never "priced + unpriced = wakes"; there was a third bucket with no name. Falsify: `wake_count − cost_reported − unpriced_sessions` from `musterd report --json` over a window with codex wakes in it — if the floor saw them, that difference is 0.

Why it stayed invisible for three weeks: `codex.ts` wrote **no settle line at all** — the `run for <seat> (fresh) settled: exit=N cost=$X wall=Ns` line that made the claude backend measurable by `grep` did not exist for codex, so there was nothing in host.log to notice was missing. An instrument's silence on one arm looks like that arm being quiet, not like the instrument being deaf ([instrument silence](instrument-silence.md)).

Why it matters beyond the number: gptbot entered a wake loop on 2026-09-01 (six leases in ~40 minutes on two acceptance asks, each session reaching progress 0.1 and dying without discharging the act — stanley, lane 01M1G310Y7). ADR 252 exists so that repeated waking shows its price before someone pays it. The seat that cannot finish a turn is also the seat that cannot report what the turn cost. And gptbot is the team's cross-family seat, so any cross-family cost comparison off this ledger (ADR 056) was biased in favour of the family that fails to report.

Fixed 2026-09-02 (lane 01M1G310Y7): every settled run on all three child-process backends now carries host-measured `duration_ms`, and codex/opencode write the same `settled:` line as claude. `cost_usd` on codex stays **absent, not fabricated** — ADR 252 §Decision refused a plausible number on a path with no cost source, and that still holds: Codex 0.150.1 ships no documentation of its `--json` usage event, so a price for it is a separate lane once the shape is confirmed from a real run. What the fix guarantees is narrower and true: every wake that spawned a process lands on the rail with its wall clock, so a loop of six shows as six rows, not zero.

## The actuator killed its own codex wakes for six days — the env channel stops at the harness (2026-09-02, lane 01M1HM8EEK, ADR 354; falsify: `sqlite3 ~/.musterd/musterd.db "select json_extract(detail,'$.reason'), count(*) from audit where target='gptbot' and action='residency.wake_deferred' and ts > (strftime('%s','now')-3*86400)*1000 group by 1"`)

gptbot's last `residency.woke` is 2026-08-27. Every wake after it ended the same way: the codex session came up, claimed the seat through the MCP, started the work — and at the verify window the actuator recorded `the seat is held by another session (provenance session, not lease …)` and killed it. Then the corpse's transcript, under ten minutes old, made the next lease defer `local-session-live`; the one after that repeated the kill. In the three days to 2026-09-02: 0 woke, 13 held-by-another, 11 local-session-live. The one on 2026-09-02 10:45 was caught in the act: 89 rollout events, `team_wake_context` → `team_next` → `lane_board` → `git show` → `vitest run`, killed four seconds after its last tool call. That is the 2026-09-01 "wake loop" on the acceptance asks — not a loop in the agent, a loop in the actuator.

**Mechanism.** `codexWakeEnv` puts `MUSTERD_PROVENANCE=wake` and `MUSTERD_WAKE_LEASE` in the codex child's env, correctly. Codex launches its MCP stdio servers with a sanitized environment — measured on codex-cli 0.150.1 by pointing a throwaway `mcp_servers` entry at `sh -c 'env > file'`: **twelve variables** (`HOME PATH USER SHELL TMPDIR LANG LOGNAME TERM` + 4), no `MUSTERD_*`. So the adapter attests `provenance: session` and no lease; ADR 241 reads not-mine; the codex backend's not-mine path calls `killTree` before it returns `deferred`. Claude Code forwards the env to its MCP servers, which is the entire reason the four claude-code seats never saw this. Codex hooks are not a bypass: the audit for that session has `claim.occupied surface=codex` and no hook row.

**Why it took six days to see.** Every symptom pointed elsewhere. The audit said "held by another session", which reads as a human in the seat. The next symptom was "local-session-live", which reads as the same. The priced rail showed nothing because the same seat could not report cost ([above](#the-third-cause--the-backend-never-produced-a-completion-and-the-floor-cannot-see-it-2026-09-02-lane-01m1g310y7-falsify-grep--c-wake-cost-recorded-for-gptbot-musterdhostlog-against-grep--c-portable-delivery-for-gptbot)). And gptbot's own status updates during those wakes ("orienting on the lane, progress 0.1") were true — it *was* working — right up to the kill. Two instruments each said "someone else is there"; nobody was.

**Fixed 2026-09-02 (ADR 354).** The actuator also writes `<workspace>/.musterd/wake-lease.json` at spawn, naming the child's pid; the adapter reads it only when the env is silent on both provenance and lease, and honours it only when the file names a process in its own bounded ancestry and is unexpired. Env-first everywhere, so Claude Code is untouched; the pid binding is what keeps a human session in the same workspace from inheriting a lease (ADR 236's line).

~~honours it only when the file names its own parent~~ CORRECTED 2026-09-02 by the first live falsifier: the fix as merged in #1187 checked the *parent* only, and the first gptbot wake on that build (lease `01M1HXRG…`, 13:43) was refused on the pid — because `codex` on this machine is `@openai/codex/bin/codex.js`, a Node wrapper that `spawn`s the native binary, so the actuator's child is the MCP server's grandparent. The wake itself was the first successful woken codex turn since 08-27 — gptbot posted its status at 13:43:15 and its `accept` at 13:43:18, eight seconds before the verify window mis-labelled it held-by-another at 13:43:26 — so the mis-label cost a false deferral row, not the errand. Replaced by a six-hop ancestry walk (`processAncestry.ts`), one `ps` per hop, run only after a file is found and unexpired. Falsify: the next gptbot lease on a build carrying the walk produces `residency.woke` with `lease_matched: true` and no held-by-another row. CONFIRMED 2026-09-02 14:19 on a629f8ea (#1192): gptbot lease `01M1HZTGA5M4RTCSJXFAXK6GBK` → `residency.woke` (`⚡ woke gptbot: session=resumed … provenance=wake`), status + accept at 14:19:13, cost row, no deferral — the first `residency.woke` for gptbot since 2026-08-27. **Trap for anyone binding to a spawned pid:** check what the executable on `PATH` actually *is* — a symlink to a `.js` launcher means your child is one generation short of the process you think you spawned. Falsify the fix: the next gptbot lease minted after the host actuator is on the fixed build (`cat /Users/nick/agents/packages/cli/dist/build.json` — the actuator runs THAT dist, not any workspace's) produces `residency.woke` with `lease_matched: true` and no held-by-another row.

**Still true, and not fixed here:** the actuator will kill a session it spawned ninety seconds earlier, in its own workspace, whose thread id it just wrote to `binding.json`, on the strength of one missing env var. It has the evidence to know better and does not consult it. ADR 354 §Consequences names it for a sibling lane.

## The mirror defect — claude-code never read `lease_matched` at all (2026-09-02, lane 01M1HQC9JJ, ADR 241 inc 3; falsify: `grep -c lease_matched packages/cli/src/host/backends/claudeCode.ts` on a checkout before this lane landed — it prints 0)

The codex bug [above](#the-actuator-killed-its-own-codex-wakes-for-six-days--the-env-channel-stops-at-the-harness-2026-09-02-lane-01m1hm8eek-adr-354-falsify-sqlite3-musterdmusterddb-select-json_extractdetailreason-count-from-audit-where-targetgptbot-and-actionresidencywake_deferred-and-ts--strftimesnow-386400001000-group-by-1) was a lease that never reached the adapter, so a true occupant read as foreign. The claude-code backend had the opposite gap: it gated on `verified.occupied` alone and never read `lease_matched`, so a FOREIGN occupant read as its own (2026-09-02; falsify: `git show b0a18f78:packages/cli/src/host/backends/claudeCode.ts | grep -c lease_matched` prints 0, and the same for `claudeCode.test.ts`). `grep -c lease_matched backends/*.ts` before the fix: codex 3, opencode 3, native 2, grok 2, claudeCode 0. ADR 241 §Limitations had named this as "increment 3 (open)" on 2026-08-07 and it stayed open for 26 days, with "no symptom yet only because its wakes have not been contended" — the seats that are contended (a human sitting in the worktree while a wake fires) are exactly the four claude-code seats.

**What it did.** A wake into a seat a human already held: the presence row is the human's, `occupied` is true, the backend logged `⚡ woke`, reported the act delivered, and left the spawned `claude -p` child running detached in the same worktree beside the human's session — the ADR 068 displacement it exists to prevent. The act was never received by anyone. On the resume path the same misread ALSO skipped the deferral: a resume that found a foreign row counted as success rather than falling through, so the case never even reached the "held by another" branch it did not have.

**Why the tests were green.** `claudeCode.test.ts` stubbed `verifyOccupied` as `{ occupied: true, provenance: 'wake' }` — no `lease_matched` field, on all 30 occupied stubs — so the stub shape itself could not express the failing case. That is the [boundary-injection trap](boundary-injection.md) in its test-fixture form: a fixture that omits a field cannot fail on that field. The red test that drove the fix is `{ occupied: true, provenance: 'session', lease_matched: false }` ⇒ `deferred: true`, child killed, no fresh fallback.

**Fixed 2026-09-02 (lane 01M1HQC9JJ).** `occupied && !lease_matched` now defers with `the seat is held by another session (not lease …)`, kills the child, and a deferred resume returns without a fresh fallback. The "MCP dist may predate ADR 131 inc 3 (rebuild it)" note now fires only inside the lease-matched branch — a foreign row is not a stale dist, and that note cost an hour of misdirection on 2026-09-02. Falsify the fix live: fire a wake at a claude-code seat whose worktree a human session holds; the audit must show `residency.wake_deferred` with that reason, no `residency.woke`, and no second `claude` process in the worktree.

**Observed 2026-09-02 15:21 on the live actuator (build 11b0cffe, which carries #1193).** Staged on sloane, enrolled for the test and unenrolled after: a `steer` minted lease `01M1J3AGB8K264SXTMKNPH55WS` at 15:20:06; a CLI presence for sloane (provenance absent, no lease — the foreign row) was refreshed every ~8s from the worktree, and the wake's own `claude` child was killed 0.4s after spawn so it could never attest. At the verify deadline the actuator wrote `residency.wake_deferred` "the seat is held by another session (not lease 01M1J3AGB8…)" (15:21:44), then `wake_cost` with the host wall clock; no `residency.woke`, no attempt charged, and the only process left in the worktree was the staging script. Falsify: `sqlite3 ~/.musterd/musterd.db "select action, json_extract(detail,'$.reason') from audit where json_extract(detail,'$.lease_id')='01M1J3AGB8K264SXTMKNPH55WS'"`. Two things the staging taught: the deferral branch is reachable only when the wake's own child fails to attest inside the window — with both rows present the child's lease wins and the outcome is `woke` beside a foreign occupant, which is the ADR 068 displacement this lane does not close — and `request_help` rides the batched lane, which a 30-minute cooldown after any `wake_failed` silences, so a falsifier that needs a lease now sends a `steer`.

## The host registry said a seat was not here while it was — two spellings of one daemon (2026-09-02, lane 01M1J2V4EJ; falsify: `sqlite3 ~/.musterd/musterd.db "select datetime(ts/1000,'unixepoch','localtime'), json_extract(detail,'$.reason') from audit where target='sloane' and action='residency.wake_failed' and ts between 1788386700000 and 1788387000000"` — two rows, 46s apart)

Enrolling sloane (`musterd residency on --seat sloane --as nick`, run in its worktree) wrote a host-registry entry whose `server` was `http://localhost:4849`, copied verbatim from that workspace's `binding.json`; every sibling entry said `http://127.0.0.1:4849`. The host loop groups its lease polls by the (server, team, host-label) STRING, so sloane polled in a group of one. Both groups asked the same daemon for the same host label; the siblings' group claimed sloane's lease first, looked for sloane in ITS entries, and reported `residency.wake_failed` "seat not in this machine's host registry — re-run `musterd residency on` in its workspace" (2026-09-02 15:05:37 and 15:06:23). The seat was registered; the advice had been followed; the instrument said otherwise. Repair that made the third lease spawn: editing the entry to `127.0.0.1`.

**The second defect the first exposed.** `wakesSince` counts `residency.wake_failed` toward `hourly_cap`, and `attemptsForAct` counts it toward `attempt_cap` — so two failures that spawned nothing and cost nothing spent sloane's 2/h and 2 of the act's 3 attempts, on a diagnosis that named the wrong thing (2026-09-02; falsify: `select count(*) from audit where target='sloane' and action='residency.wake_failed' and ts between 1788386700000 and 1788387000000` is 2, and the seat's policy showed `2/h`). loop.ts's own comment on the branch says three such failures "exhaust the act on a diagnosis that names the wrong thing" — and then reported them as failures anyway. A pre-actuation fault is a property of the machine, not of the act (ADR 221's line): nothing was paid and the fix is a local command.

**Fixed 2026-09-02 (lane 01M1J2V4EJ).** `canonicalServer` folds case, the trailing slash and every loopback alias; the registry stores it on write, `residency off` matches through it, and the loop groups on it (entries written before the fix are folded at read). The two pre-actuation branches ("not in registry", "workspace missing or unbound") now report `deferred: true` with the ADR 189 wakeability, audited as `residency.wake_deferred` and charged nothing, with an explicit `wake deferred:` host-log line so the rail stays loud. Falsify the fix: two registry entries differing only in `localhost` vs `127.0.0.1` produce ONE lease poll (`loop.test.ts`), and the sloane query above returns no `wake_failed` rows for any lease minted after the actuator carries this build.

**Residual, named:** a seat genuinely enrolled on no reachable host now defers every 5 minutes forever instead of exhausting after three loud failures. That is the trade ADR 221 already made for a missing binary; dolly's lane 01M1J1KC (roster `wakeable` reads only "a residency row exists") is where that seat should become visible at read time rather than at wake time.
## The roster's `wakeable` was enrolment, labelled as reachability (2026-09-02, ADR 357; falsify: `launchctl unload` the host LaunchAgent and read `/live` after 70 s — before ADR 357 every enrolled seat still says `wakeable`)

`GET /teams/:slug/members` set `wakeable: residency.has(member)` — a row exists — and that was the whole derivation. ADR 189's five-state `wakeabilityFromFacts` had been called with two of its four facts since it was written: nothing ever supplied `host_reachable` or `workspace_readable`, so `enrolled_host_stale` and `enrolled_dead_workspace` could appear only on a wake REPORT after the wake had failed. The daemon had no host liveness signal at all — while `musterd host` had been polling `POST /residency/wake-leases`, naming its host, every 10 s since ADR 131. A heartbeat received for two months and never written down.

Why it bit: ADR 191 routes an acceptance to "a marked-wakeable offline seat". A dead actuator or a deleted workspace kept its seats reading `wakeable`, the ask waited on a wake that failed, and the failure lived in host.log — the [codex kill](#the-actuator-killed-its-own-codex-wakes-for-six-days--the-env-channel-stops-at-the-harness-2026-09-02-lane-01m1hm8eek-adr-354-falsify-sqlite3-musterdmusterddb-select-json_extractdetailreason-count-from-audit-where-targetgptbot-and-actionresidencywake_deferred-and-ts--strftimesnow-386400-1000-group-by-1) one layer up.

Fixed by ADR 357: the poll IS the heartbeat (`host_liveness`, newest wins), `seatWakeabilityFacts` derives the two missing facts once for the roster and the ADR 191 pool alike, `MemberSummary.wakeability` carries the enum beside the unchanged boolean, and `/live` renders `enrolled · host quiet` / `workspace gone` / `busy`. **Trap kept from the first draft:** "never seen ⇒ stale" turned five existing wake-pool tests red — a host this daemon has never heard from is *unknown*, not stale (ADR 236), and unknown never demotes; only an observed silence does.

~~`HOST_STALE_MS` = 60 s, six missed 10 s polls~~ CORRECTED 2026-09-02 15:51 by the first live falsifier, twenty minutes after #1197 landed: the actuator polls every 10 s only while **idle**. `pollHostOnce` is serial — an actuation suspends polling for its whole verify window — and the first codex wake on the new build went 94 s between requests (15:50:02 poll → 15:51:36 progress) while doing exactly what it should. At the baseline read, before I had touched anything, every enrolled seat already said `enrolled_host_stale`. Now 5 min, and every host-authenticated residency request (poll, progress, turn, report) is a sighting, so a busy host refreshes at report time. **Trap:** "polls every N seconds" is an idle-cadence claim; measure the silent stretch under load before you set a staleness line on it. Falsify the corrected line: `enrolled_host_stale` on any seat while `wake_leases` holds an unsettled lease for its host younger than 5 min.

## What codex and opencode actually print at turn end — tokens, no attestable price (2026-09-02, lane 01M1HJY3JF, ADR 364; falsify: `codex exec --json --skip-git-repo-check "Reply ok" | tail -1` and `opencode run --format json "Reply ok" | tail -1`)

Captured from one real run each on this machine, not from docs (codex ships none): `codex exec --json` 0.152.1 ends with `{"type":"turn.completed","usage":{input_tokens, cached_input_tokens, cache_write_input_tokens, output_tokens, reasoning_output_tokens}}` — no model, no price, and the seat is on a ChatGPT subscription so no marginal price exists. `opencode run --format json` 1.18.27 ends each step with `part.tokens {input, output, reasoning, cache{read, write}}` and `part.cost` — which printed a literal `0` for a paid provider. Since ADR 364 the `residency.wake_cost` row carries the tokens as `usage`, the reason as `unpriced_reason`, and opencode's figure as `harness_cost_usd` (its claim, never in a total); `cost_usd` stays absent on both. The parsers are pinned to the captured lines verbatim in `codex.test.ts` / `opencode.test.ts` — if upstream changes the shape, re-capture from a real run before touching the schema.

## Test trap

`claimWakeLeases` MUTATES — it claims a lease, so a second call on the same db is suppressed by the live lease and cooldown, and a before/after test on one db passes before any fix exists. Use a fresh seed per assertion.

## Related

`claimWakeLeases` already reasons per-act (`isExhausted` keyed on act_id) — an act-scoped gate mirrors `liveLease` keyed on `act_id` instead of `member_id`. A live seat is never woken (`hasLivePresence` guard), so "wake only if none live" is free.

## A wake crosses machines by being read, not by being sent (2026-09-02, lane 01M1JBQRA3; falsify: `sync/claim.test.ts` "folded from another machine")

ADR 361 §Consequences said a handoff decided on one machine for a seat resident on another "spends no wake lease anywhere, because the fold does not consult the wake ledger". Measured 2026-09-02 on two real daemons: **false.** There is no wake ledger to consult at fold time — `claimWakeLeases` (`store/residency.ts`) runs on the host actuator's poll and derives candidates from `messages` (`listInterruptCandidates` for the immediate lane, `openDirectedLedger` for the batched lane, both plain queries over acts and their `accept`/`decline`/`resolve` replies). A folded row is a row. So a handoff minted on the hub for a seat enrolled on the joiner leases exactly one wake on the joiner's next poll and none on the hub, where the seat has no residency row; and the recipient's folded `accept` drops the act from the sender's open ledger. What does NOT cross: the residency row and the lease (residence 3) — a seat is woken only where it is enrolled, which is the point.

Falsify: enroll a seat on a joiner, hand it a lane from the hub, `pullTeam` on the joiner, then `claimWakeLeases(joiner.db, …)` — anything but one order for that act, or any order on the hub, breaks this.
