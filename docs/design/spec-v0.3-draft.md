# musterd protocol — SPEC v0.3 draft → moved

> **This content has moved.** The v0.3 shared-teams governance wire-design now lives in **`SPEC.md` → Appendix A — Unreleased** (one spec, no live/draft split — see AGENTS.md → "Where each doc lives" for why). The design rationale and review are in `docs/design/membership-model.md` + `docs/design/security.md`; the capability *two-universes* split is ADR 026.

This file is kept only as a redirect so existing links resolve. New references should point at `SPEC.md` Appendix A.
