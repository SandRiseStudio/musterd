{
  "name": "musterd",
  "version": "0.0.0",
  "description": "Muster your agents and humans into persistent teams — across any harness, with a shared protocol. Name reservation; the real CLI ships from https://github.com/ (see repository).",
  "license": "MIT",
  "keywords": ["agents", "multi-agent", "coordination", "mcp", "teams"],
  "files": ["README.md"],
  "publishConfig": {
    "access": "public"
  }
}
